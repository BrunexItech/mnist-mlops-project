project:
  name: "mnist-mlops"
  version: "0.1.0"

data:
  input_shape: [28, 28, 1]
  num_classes: 10
  validation_size: 0.2
  random_state: 42

model:
  batch_size: 32
  epochs: 10
  learning_rate: 0.001

mlflow:
  tracking_uri: "./mlruns"
  experiment_name: "mnist_classification"